#server.port=9009
#logging.config: classpath:logback.xml
server.servlet.context-path: /pns
app:
   switch:
      index.update:
         customer: true
         contract: true
         equipment: true
         equipmentDetails: true
   index:
      name:
         customer: customers
         contract: contracts
         equipment: equipments
         equipmentItem: equipmentItems